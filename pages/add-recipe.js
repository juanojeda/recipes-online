import React from "react";

import { Editor } from "../components/Editor";

const AddRecipe = () => {
  return (
    <Editor exTitle="" exIngredients={[]} exMethods={[]} isEditMode={false} />
  );
};

export default AddRecipe;
